import { dev } from '$app/environment';

export const title = 'runnin dev';
export const description = 'Getting back to running';
export const url = dev ? 'http://localhost:5173/' : 'https://runnin.dev';
